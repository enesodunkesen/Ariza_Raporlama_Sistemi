namespace Ariza.Core.Entities
{
    public enum FaultStatus
    {
        Pending = 0,
        InProgress = 1,
        Resolved = 2
    }

    public enum MaintenanceStatus
    {
        Pending = 0,
        InProgress = 1,
        Completed = 2
    }
}
